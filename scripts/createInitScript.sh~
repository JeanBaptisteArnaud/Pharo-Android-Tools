#!/bin/sh

echo "#!/bin/sh
export PATH=\$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$ANDROID_NDK
export ANDROID_HOME=$ANDROID_HOME
export ANDROID_NDK=$ANDROID_NDK" >> EnvironmentVariable.sh
chmod +x EnvironmentVariable.sh


